# Anthropic / OpenAI — oracle backend
OPENAI_API_KEY=

# Supabase — optional. Set both to enable persistence.
# Server-side only; NEVER prefix with VITE_ or expose to the client.
SUPABASE_URL=https://<your-project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
